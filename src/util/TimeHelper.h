#ifndef PIPEY_TIMEHELPER_H
#define PIPEY_TIMEHELPER_H

#include "../common/common.h"

namespace pipey {
	namespace util {

		void SleepSeconds(uint32_t nSeconds);
		void SleepMilliseconds(uint32_t nMilliseconds);

	}
}

#endif